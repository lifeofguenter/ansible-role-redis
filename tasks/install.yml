---

- name: Install redis
  ansible.builtin.apt:
    name: redis
    state: present
    default_release: '{{ ansible_distribution_release }}-backports'
    update_cache: yes
    cache_valid_time: 3600
  become: yes
